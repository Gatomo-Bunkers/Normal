import { Command } from '../../types';
import Disabled from '../../db/models/disabled';

export const command: Command = {
	name: 'disable',
	description: 'Desactiva un comando',
	run: async (client, msg, args) => {
		
	}
}